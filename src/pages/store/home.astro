---
// Fundations
import BaseLayout from "@/layouts/BaseLayout.astro";
import Text from "@/components/fundations/elements/Text.astro";
import Wrapper from "@/components/fundations/containers/Wrapper.astro";
// Components
import StoreEntries1 from "@/components/store/StoreEntries1.astro";
// Content
import { getCollection } from "astro:content";
const allProducts = await getCollection("store");
---

<BaseLayout>
  <section>
    <Wrapper variant="standard" class="py-24">
      <div class="text-center">
        <Text
          tag="h1"
          variant="displayXL"
          class="dark:text-white text-base-900 text-balance"
        >
          Digital products <span
            class="italic font-light tracking-wide font-display block"
            >for your business</span
          >
        </Text>
        <Text
          tag="p"
          variant="textLG"
          class="mt-8 dark:text-base-400 text-base-600 text-balance"
        >
          Shop my selection of digital products to help you grow your business,
          and to make your life easier and more productive.
        </Text>
      </div>
      <div class="flex flex-col gap-2 mt-12">
        {allProducts.map((post) => <StoreEntries1 post={post} />)}
      </div>
    </Wrapper>
  </section>
</BaseLayout>
